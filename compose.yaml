services:

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: secret123

  api:
    build: .
    image: predict-api:ci
    ports:
      - "8000:8000"
    environment:
      DB_HOST: db
    depends_on:
      - db